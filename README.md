# October CMS Support

Base PhpStorm plugin scaffold for future October CMS support features.

## Build

```powershell
.\gradlew.bat buildPlugin
```

The built plugin ZIP is written to `build/distributions/`.
